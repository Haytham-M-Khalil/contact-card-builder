// =============================================================================
//  Contact Card Builder — app logic
//  ---------------------------------------------------------------------------
//  Browser-only. Plain JS, classic <script> (no modules, no build step).
//  Loaded AFTER the vendored libs in lib/ (qrcode-generator, jsPDF).
//
//  Roadmap (one small, testable piece per commit — see CLAUDE.md):
//    2. form UI            3. vCard data layer      4. QR (.png)
//    5. PDF (vector QR)    6. file toggle/downloads 7. polish
// =============================================================================

(function () {
  "use strict";

  // TODO (step 2+): read the form, build the vCard, generate QR + PDF, wire downloads.
  console.log("Contact Card Builder: scaffold loaded.");
})();
